namespace CMDataCollector.Connection
{
    public class ConnectionManager
    {
        //private Dictionary<string, CMConnection> _cm = new Dictionary<string, CMConnection>();
        //private int _connCount;
        //private static ConnectionManager _instance;
        //public static ConnectionManager GetInstance()
        //{
        //    if (_instance == null)
        //        _instance = new ConnectionManager();

        //    return _instance;
        //}

        //private ConnectionManager()
        //{
        //}

        //public void GetOrCreateConnection(string key)
        //{
        //    if (!_cm.ContainsKey(key))
        //    {
        //        _cm.Add(key, new CMConnection());
        //    }
        //}

        //public static string GenerateUniqueKey()
        //{
        //    return Guid.NewGuid().ToString();
        //}
    }
}
